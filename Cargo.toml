[package]
name = "blog-generic"
version = "0.0.4"
edition = "2024"

[dependencies]
serde = { version = "1.0.160", features = ["derive"] }
validator = { version = "0.16.1", features = ["derive", "unic", "phone"], optional = true }

[features]
default = []